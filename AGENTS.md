# AI/開発者向け指示書

このファイルには、本プロジェクト（Text Toolkit）に機能やツールを追加する際、AIエージェントまたは人間の開発者が厳格に遵守すべきUI、アーキテクチャ、実装ルールを定義しています。

---

## 🎨 1. UI・スタイルルール

- **カラーシステム**:
  - メインのブランドカラーはインディゴ（Indigo: `--c-primary`）です。
  - ライトモードとダークモードで視認性を保つため、CSS変数が定義されています。色コードを直書きせず、必ず `style.css` に定義された変数を利用してください。

---

## 🏗 2. アーキテクチャと共通モジュールルール

- **モジュール化**:
  - 複雑な処理や再利用するロジックは、個別のファイルに直書きせず、`modules/` 配下に切り出してください。
- **多言語対応（i18n）の必須化**:
  - UIに表示されるすべてのテキスト（タイトル、説明文、ボタンラベルなど）は `modules/i18n.js` の `translations` オブジェクトに追加してください。
  - HTML内にUIテキストを直書きせず、必ず i18n のキーを `data-i18n` 属性などで参照する形にしてください。

---

## 📦 3. リリース・バージョン管理ルール

- **キャッシュのバージョン管理 (`sw.js`)**:
  - Service Workerで使用するキャッシュ名 (`CACHE_NAME`) は、以下の規則で運用してください。
  - フォーマット: `text-toolkit-YYYY.MM-rN` (例: `text-toolkit-2026.09-r5`)
  - ファイル追加やロジック変更などを行った際は、必ず `sw.js` 内のこのバージョン文字列を更新してください。

---

## 📝 4. コミットルール

AIエージェントおよび人間の開発者がコミットを作成する際は、必ず以下のルールに従ってください。

### 4.1. 手順
1. `git status` で変更状況を確認する。
2. 過去のコミットを10件取得してスタイルを確認する（`git log -10` などを使用し、`--oneline` は使用しないこと）。
3. 変更内容（diff）を確認し、必要に応じてテストを実行する。
4. 下記のスタイルに従ってコミットする。
5. **絶対に `git push` は実行しないこと。**

### 4.2. コミットメッセージのスタイル

```plaintext
feat: ここに大まかな内容

ここに、具体的な説明。

- `ファイル名`: 詳細な単一の説明。
- `ファイル名2`:
  - 詳細な複数の説明1。
  - 詳細な複数の説明2。
```

- `feat` の部分は変更内容に応じて Conventional Commits に従うこと。
- ファイル名はバッククォート（\`）で囲むこと。
- 具体的な説明は単一行にし、詳細な説明が複数ある場合は箇条書きで行を分ける。箇条書きの間に不要な空行は入れないこと。
- 変更内容の説明は硬い説明調を避け、「〜しました」「〜対応しました」「〜に変更しました」など、自然な完了報告の表現にすること。